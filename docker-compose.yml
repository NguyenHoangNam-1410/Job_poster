services:
  # MySQL Database Service
  db:
    image: mysql:8.0
    container_name: worknest-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_NAME:-job_poster}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_USER: ${DB_USERNAME:-job_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-job_password}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./config/init_db.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./config/create_db.sql:/docker-entrypoint-initdb.d/02-create.sql
    networks:
      - worknest-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # PHP Apache Service
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worknest-web
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_NAME: ${DB_NAME:-job_poster}
      DB_USERNAME: ${DB_USERNAME:-job_user}
      DB_PASSWORD: ${DB_PASSWORD:-job_password}
      BASE_URL: ${BASE_URL:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-noreply@worknest.com}
      MAIL_FROM_NAME: ${MAIL_FROM_NAME:-WorkNest}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www/html
    networks:
      - worknest-network
    stdin_open: true
    tty: true

volumes:
  db_data:
    driver: local

networks:
  worknest-network:
    driver: bridge

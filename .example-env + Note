GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID

SMTP_EMAIL=YOUR_SMTP_EMAIL
SMTP_PASSWORD="YOUR_SMTP_PASSWORD"
SMTP_PORT=587 (Or another if conflicted)

FACEBOOK_CLIENT_ID=YOUR_FACEBOOK_CLIENT_ID
FACEBOOK_CLIENT_SECRET=YOUR_FACEBOOK_CLIENT_SECRET
FACEBOOK_REDIRECT_URI=YOUR_FACEBOOK_REDIRECT

( Need to be https for facebook login to work)
Also need composer:

# If already have composer.json with the needed dependenciess,
# Just install it
composer install

# If no composer.json, need to init it:
composer init
composer require phpmailer/phpmailer
composer require vlucas/phpdotenv
composer require google/apiclient

cd 'C:\xampp\htdocs\Worknest'; & 'C:\xampp\php\php.exe' composer.phar require phpmailer/phpmailer --ignore-platform-reqs